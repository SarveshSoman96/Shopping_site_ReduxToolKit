import React from 'react'

const Loading = () => {
  return (
    <h3 style={{color: "#000", fontStyle: "italic", textAlign: "center", marginTop: "2rem"}}>Products are Loading...</h3>
  )
}

export default Loading;